{
  "name": "@valudio/native-clipboard",
  "version": "0.0.1",
  "description": "Native node addon that provides tools to manage the clipboard on macos and windows",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Marc Fuentes Montenegro <marc.fuentes@valudio.com>",
  "license": "MIT",
  "keywords": ["node", "addon", "clipboard", "electron"],
  "scripts": {
    "build": "neon build --release && tsc && mv ./native/index.node ./dist/index.node",
    "start": "node ./lib/index.js"
  },
  "devDependencies": {
    "@types/node": "^12.7.5",
    "neon-cli": "0.3.1",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  }
}
